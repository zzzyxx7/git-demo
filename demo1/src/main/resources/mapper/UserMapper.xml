<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.demo1.mapper.UserMapper">

    <!-- 结果映射配置 - 使用完整类路径 -->
    <resultMap id="UserResultMap" type="org.example.demo1.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="age" column="age"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询所有用户 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM user ORDER BY id DESC
    </select>

    <!-- 根据ID查询用户 -->
    <select id="findById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 插入用户 - 使用完整类路径 -->
    <insert id="insert" parameterType="org.example.demo1.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, age, phone)
        VALUES (#{username}, #{password}, #{email}, #{age}, #{phone})
    </insert>

    <!-- 更新用户 - 使用完整类路径 -->
    <update id="update" parameterType="org.example.demo1.entity.User">
        UPDATE user
        SET username = #{username},
            password = #{password},
            email = #{email},
            age = #{age},
            phone = #{phone}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除用户 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 统计用户数量 -->
    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user
    </select>

    <!-- 分页查询用户 -->
    <select id="findByPage" resultMap="UserResultMap">
        SELECT * FROM user
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

</mapper>